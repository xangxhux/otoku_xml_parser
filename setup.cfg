[metadata]
name = otoku-xml-parser
version = 0.0.1-alpha.1
description = An xml parser for the JMDict, KanjiDIC and related files.

[options]
package_dir=
    =src
packages = find:

[options.packages.find]
where=src